{
  "presets": [
    [
      "@babel/preset-env",
      { "targets": "> 0.25%, not dead" }
    ],
    [
      "@babel/preset-react",
      { "runtime": "automatic" }
    ]  // `runtime: 'automatic'` enables the new JSX transform
  ]
}
